# Learning Distiller Digest — 2026-07-04

Run: 2026-07-05T04:14Z (first run; no prior cursor — processed all events from start of `events.jsonl`).
Inputs: 4 events (`events.jsonl`), 2 legacy `oracle/_raw/*.md` files, `graphify-out/reflections/LESSONS.md` (empty — no marked outcomes).

## Promoted (1)

- **h_mr7a2arj_5nsh** (global store, category `orchestration`, basis `directly-observed`):
  "When dispatching a worker to write typebox schemas in ~/.pi/agent extensions, name
  extensions/heuristics/schema.ts as the schema-style reference in the spec — omitting the
  repo's StringEnum convention yields Type.Union(Type.Literal) boilerplate that fails house
  lint (caused two consecutive peer FAILs)."
  Source: verdict FAIL cluster `ev_19f307a5f2a_test` + `ev_19f307a5f2b_test` — same findings
  shape twice (spec omitted the StringEnum convention), the highest-value promotion class.

## Reinforced (2)

- **h_mr6xbomn_tas8dzud** (project store `~/.pi/heuristics/`): void-splash harness via
  `node --experimental-strip-types extensions/_void_harness.mts`. hits 0→1. Source: explicit
  event `ev_19f307a5f29_test` — semantic near-match, no duplicate created. (Also overlaps
  global h_mr5t3f3y_86jv0cg9; reinforced the closer, project-scoped match only.)
- **h_mr70zwby_8j9fhrrc** (agent global store): "_raw backlog is raw graph BFS dumps —
  delete rather than promote". hits 0→1. Corroborated by tonight's `_raw` drain (both files
  were exactly that).

## Dropped (3)

- `ev_19f307a5f2c_test` (query): answer starts "Traversal: BFS" — regenerable raw node dump,
  filtered per SCHEMA.md.
- `oracle/_raw/graph-20260705-003423-0-how-does-pi-self-improve…md`: raw BFS node list
  (115 nodes), no distilled prose; regenerable from the graph. Judged noise, file deleted.
- `oracle/_raw/graph-20260705-003423-1-mechanisms-to-prune-refactor…md`: raw BFS node list
  (456 nodes, truncated mid-line), same class. Judged noise, file deleted.

## Disputed (0)

None. No contradictions with existing oracle pages or heuristics found.

## Stats

- Events in: 4 (explicit 1, verdict 2, query 1, rework 0, correction 0, violation 0)
- Clusters formed: 1 (the two verdict FAILs)
- Promotions: 1/10 cap · Reinforcements: 2/20 cap · Overflow: none
- Legacy `_raw` drained: 2 files deleted
- Oracle pages written: 0 (verdict cluster fit a one-line heuristic; no richer structural
  knowledge survived filtering)
- Cursor advanced to `ev_19f307a5f2c_test`

## Verification

BLOCKED: the final verification step (`audit-pipelines.py` + JSONL parse checks) was denied
by the environment's command-permission guard and was NOT re-attempted, per the guard's
instruction. All mutations in this run completed successfully before the block (heuristic
edit scripts exited 0; digest and cursor written). The JSONL stores were rewritten via
json.dumps of parsed records, so parse validity is expected but UNVERIFIED this run — the
next session-start self-audit / `/audit` will confirm.
