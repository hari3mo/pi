# Learning Distiller Digest — 2026-07-10

Run: nightly cron. Cursor in: `ev_mr8i52eq_mpma` (2026-07-08T18:12:00Z).
Events read: 21 (lines after cursor). Legacy `_raw`: none (`wiki/_raw/` empty).
`graphify-out/reflections/LESSONS.md`: 0 marked outcomes (nothing to drain).

## Promoted (pi domain → agent heuristics store) — 4

1. **orchestration** `h_mresw1at_gpuo` — Pin delegated subagent thinking effort by
   model family, never by inheriting the lead's effort: Opus and GPT-5.5 run
   `:xhigh`, Sonnet and Gemini Flash run `:high`. (basis: user-confirmed;
   from `ev_mrdzwgmo_j32z`, AGENTS.md:137 + subagent/index.ts:45)
2. **workflow** `h_mresw1bb_krp2` — When a pi update wipes the pi-tui scrollback
   fix (jumping scrollbar returns, self-audit WARNs on missing patch markers),
   re-apply with one command: `node ~/.pi/agent/patches/pi-tui-scrollback-fix-apply.mjs`,
   then run the harness — no manual dist/ editing. (basis: reproduced; from
   `ev_mre0cjlh_5r1l`, corroborated by doctor verdict `ev_mrdzzk6y_1wyk`)
3. **correction** `h_mresw1bt_xxjw` — Harimo familiar renders as a status widget
   only when paired with the void landing page; the void/harimo wordmark (not the
   familiar sprite) stays as the prompt-page header. (basis: user-confirmed;
   from `ev_mrekrdct_upof`)
4. **gotcha** `h_mresw1cb_lfby` — When rendering user-controlled strings
   (HERMES_USER / username env vars) into a TUI banner/splash, strip control/ANSI
   characters even on the "ANSI-free" non-TTY path, or the name injects terminal
   escape sequences; add tests for escape- and newline-bearing names. (basis:
   directly-observed; distilled from peer FAIL findings `ev_mreebd6s_bnv9`,
   confirmed fixed by `ev_mreeof7w_2q8n`)

## Reinforced — 0

No candidate matched an existing heuristic or wiki page (dedupe searched the
wiki vault + both pi heuristics stores for: scrollback, model-family thinking,
escape injection, harimo/wordmark, xhigh/Gemini-Flash — 0 hits).

## Dropped (with reasons)

- `ev_ec2_roundtrip` — malformed roundtrip probe, no kind/payload (empty payload).
- 3 `query`/graph-explain events (`updateStatusBar()`, `void-blackhole.ts`,
  `t()`) — regenerable node/edge dumps, not durable knowledge.
- `ev_mreff9y8_dsyr` — correction "undo changes made this session" (basis
  inferred); transient session command, not a generalizable rule. Correction
  quality gate: dropped.
- 2 PASS verdicts (`ev_mre2m2w8_84v6` doctor, `ev_mreeof7w_2q8n` peer) — telemetry;
  the peer PASS's substance was folded into promotion #4, not double-counted.
- doctor FAIL `ev_mrdzzk6y_1wyk` — transient (scrollback patch not yet applied);
  its durable lesson is captured by promotion #2.

## Evicted — 0

No eviction candidates: every stored heuristic is <30 days old (created
2026-07-03…07-08), so the ">30 days + hits:0 + ≥10 receipts" gate excludes all.

## Decay proposals — 0

All `source_layer: learned` wiki pages are <60 days old; none proposed for archival.

## Disputed — 0

No contradictions with existing pages.

## Stats

- Events in: 21. By kind — explicit 3, violation 9, verdict 4, correction 1,
  query 3, malformed/roundtrip 1.
- By domain — pi 18, prism 3.
- Violations by doctrine (pi, telemetry only): wiki-first 1 nudge + 1 block;
  graph-first 2 nudges + 2 blocks. (prism violations counted under the skipped
  partition, below.)
- Receipts: 173 lines present; no eviction join fired (all heuristics too young).
- Promotions this run: 4 / 10 cap. Reinforcements: 0 / 20 cap.

## Cross-reference: prism partition SKIPPED

3 prism-domain events (`ev_mre04pk2_p5pr`, `ev_mre04tpl_kjz5`, `ev_mre04yfk_6rf4`
— all graph-first violations: 1 nudge + 2 blocks) were **NOT processed**. The
prism-oracle repo (`~/prism/prism-oracle`) has a pre-existing unstaged change
(`prism-graph/.obsidian/workspace.json`, not this run's), so `git pull --rebase`
failed with "cannot pull with rebase: You have unstaged changes." Per the skill's
repo-hygiene hard rule, the prism partition is skipped entirely and left for the
next run. **Cursor is therefore held at `ev_mrdzwgmo_j32z`** — the event
immediately before the earliest unprocessed prism event (`ev_mre04pk2_p5pr`) —
even though later pi events were processed. Next run will re-see the processed pi
events; dedupe will REINFORCE rather than duplicate them.
