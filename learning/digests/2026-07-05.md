# Learning Distiller Digest — 2026-07-05

Run: nightly cron. Cursor start: `ev_19f307a5f2c_test`.
Events processed: 3 (after cursor). Legacy `_raw/`: empty. `LESSONS.md`: no marked outcomes.

## Promoted (1)

- **[heuristic · global · correction]** `h_mr8gdwp0_2j8t` — "Never write generated
  notes, summaries, or scratch output to the user's home directory — put them under the
  project's .pi/ folder instead."
  Basis: `user-confirmed`. Source event `ev_19f34b72f5a_ms2` (correction; userText:
  "no, never write summaries to my home dir — put generated notes under the project's
  .pi/ folder"). Clear generalizable preference, no existing near-match in either
  heuristics store or the oracle vault (`pi-workflow-conventions.md` covers refresh /
  task-tool / vocabulary but not output-location policy). Correction-promotion count: 1
  of ≤2 allowed.

## Reinforced (0)

- None. No candidate matched an existing heuristic or oracle page.

## Dropped (2)

- `ev_19f34b72f5b_ms2` (correction, userText "stop") — not a generalizable
  preference/rule; a one-off interrupt. Dropped per correction quality gate.
- `ev_19f34b72f59_ms2` (violation, graph-first nudge) — violations are telemetry,
  never promoted; counted in Stats only.

## Evicted (1)

- `h_synth_old_test` ("SYNTHETIC-TEST: prefer tabs over spaces in fooml files.") —
  `hits: 0`, created 2026-05-01 (>30 days ago), present in `heuristicIdsInjected` of
  all 12 receipts (≥10 threshold), not pinned. Moved to
  `heuristics/archive.jsonl` (append-only). Eviction count: 1 of ≤3 allowed.
  Note: `h_mr5k39hu_ww0o` also appears in all 12 receipts with `hits: 0` but was
  created 2026-07-03 (<30 days) — NOT an eviction candidate, retained.

## Decay proposals (0)

- All 8 `source_layer: learned` oracle pages carry `updated: 2026-07-04` (< 60 days
  old). None eligible for `lifecycle: archived` proposal this run.

## Disputed (0)

- No contradictions with existing pages.

## Stats

- Events in (after cursor): 3
  - by kind: correction 2, violation 1
- Filtered/pre-dropped: 0 (the earlier `query` node-dump `ev_19f307a5f2c_test` was
  already at/behind the cursor).
- Violations by doctrine: graph-first — 1 nudge, 0 block.
- Receipts joined: 12 (all `synthetic-r0`..`r11`). Injection tally over 12 receipts:
  `h_synth_old_test` 12, `h_mr5k39hu_ww0o` 12. Outcomes: PASS 6, null 6.
- Promotions this run: 1 (cap 10). Reinforcements: 0 (cap 20). Evictions: 1 (cap 3).
  Corrections promoted: 1 (cap 2).

Cursor advanced to `ev_19f34b72f5b_ms2`.
