# Learning Distiller Digest — 2026-07-08

Run: nightly cron. Cursor start: `ev_19f34b72f5b_ms2` (2026-07-05T23:59:45Z).
Events processed: 14 (after cursor, ids `ev_mr8h12ru_wqcv` … `ev_mr8i52eq_mpma`).
Legacy `_raw/`: none (no `wiki/_raw/` or `oracle/_raw/` dir). `LESSONS.md`: no marked outcomes.

Note: vault is `~/.pi/agent/wiki/` (the skill's "oracle" == this renamed vault; SCHEMA.md
is authoritative and already uses `wiki`). Global heuristics live in
`~/.pi/agent/heuristics/heuristics.jsonl`; project store is `~/.pi/heuristics/`.

## Promoted (1)

- **[heuristic · global · gotcha]** `h_mrcdutc0_rn1t` — "When doing a repo-wide
  terminology rename in ~/.pi/agent (e.g. oracle→wiki), also scrub residual tokens from
  non-source tracked artifacts — .bak backups, Obsidian .obsidian/workspace.json, and
  __pycache__/*.pyc bytecode — because a repo-wide residual-token invariant check still
  fails on them after every source file is renamed."
  Basis: `directly-observed`. Source event `ev_mr8hwhuf_it2u` (peer verdict
  `FAIL: implementation`): three MAJOR findings, all the same shape — stale `oracle`/`wiki`
  terminology surviving in `heuristics/.bak:26`, `wiki/.obsidian/workspace.json:190`, and
  `scripts/__pycache__/audit-pipelines.cpython-314.pyc:1` after the source rename. New,
  evidenced, generalizable; no near-match in either heuristics store or the wiki vault
  (`harness-environment-quirks.md` covers autocommit/PATH/validate-config, not rename
  residuals). Promotion count: 1 of ≤10.

## Reinforced (0)

- None. No candidate semantically matched an existing heuristic or wiki page.

## Dropped (12)

- `ev_mr8h6w8b_vpp2` (verdict PASS, void/custom-header animation) — PASS with only a NIT;
  no failure pattern or durable lesson to promote.
- `ev_mr8haiu1_wzua`, `ev_mr8haiud_oght` (query, graph `explain` of graph-first.ts /
  oracle-first.ts) — raw node/edge `explain` dumps; non-durable per the isSubstantive /
  raw-dump filter.
- `ev_mr8hkwmu_i3ic` (correction, "peer should inherit thinking level from lead like
  engineer") — a one-off config-change instruction already codified in AGENTS.md canon
  (peer inherits the lead's thinking level like engineer); not a durable cross-session
  preference, no reinforcement target. Correction quality gate: dropped.
- `ev_mr8hs3m5_p85t` (correction, "revert wordmark to how it was before any changes
  today") — one-off revert, not generalizable. Dropped per correction quality gate.
- `ev_mr8i52eq_mpma` (correction, "actually revert back i dont like tem") — one-off
  revert/preference, not generalizable. Dropped per correction quality gate.
- 7 × `violation` events (`ev_mr8h12ru_wqcv`, `ev_mr8h12ru_q19q`, `ev_mr8hc99x_ubvp`,
  `ev_mr8hn2u3_yh5u`, `ev_mr8hreo7_gc23`, `ev_mr8hrpj1_kil3`, `ev_mr8hrs7p_j50n`) —
  violations are telemetry, never promoted; counted in Stats only.

## Evicted (0)

- No eviction candidates. Every heuristic in the agent store was created 2026-07-03…07-08
  (all < 30 days old), so none meets the `hits:0` + >30d + ≥10-receipt-injection bar. (The
  36 heuristics injected in all recent receipts are recent, hence protected.) Eviction
  count: 0 of ≤3.

## Decay proposals (0)

- All 8 `source_layer: learned` wiki pages carry `updated: 2026-07-04` (< 60 days old).
  None eligible for a `lifecycle: archived` proposal this run.

## Disputed (0)

- No contradictions with existing pages.

## Stats

- Events in (after cursor): 14
  - by kind: violation 7, correction 3, query 2, verdict 2
- Filtered/pre-dropped as non-knowledge: 2 query raw-dumps, 7 violations (telemetry).
- Violations by doctrine: `graph-first` — 2 nudges, 3 blocks; `oracle-first` — 2 nudges,
  1 block. (`oracle-first` is the pre-rename name of the now-`wiki-first` doctrine.)
- Receipts joined: 52 total in `receipts.jsonl`. Recent sessions consistently inject the
  same ~36-heuristic global set (`heuristicIdsInjected`); `oraclePagesRead`/`wikiPagesRead`
  mostly empty, a few reads of `concepts/*` and `components/*`. Outcomes recorded: PASS 1,
  FAIL: implementation 1, rest null. No `learned`-layer wiki page path appeared in any
  receipt's read list, but all such pages are < 60 days old so decay does not apply yet.
- Promotions this run: 1 (cap 10). Reinforcements: 0 (cap 20). Evictions: 0 (cap 3).
  Corrections promoted: 0 (cap 2).

Cursor advanced to `ev_mr8i52eq_mpma`.
