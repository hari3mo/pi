---
description: Bypass fable orchestration — answer this prompt directly, no subagents
argument-hint: "<prompt>"
---
For THIS request only, ignore the fable orchestration doctrine (Delegation Gate, Routing, Roles, Rework Loop). Do NOT dispatch subagents or run the pipeline. Work directly — read, reason, and respond yourself, decomposing design → execute → verify inline, exactly as a direct-work lead would.

$@
